
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>LogIn</h2>
@using (Html.BeginForm())
{
    @Html.ValidationSummary(true, "Fallo del login, check los detalles");
    <div>
        <fieldset>
            <legend>Login Form</legend>
            <div class="editor-label">LOGIN</div>
            @*<div class="editor-label">@Html.LabelFor(u => u.Nombre)</div>
            <div class="editor-field">@Html.TextBoxFor(u => u.) @Html.ValidationMessageFor(u => u.Email)</div>*@
            <div class="editor-label">Password</div>
            @*<div class="editor-label">@Html.LabelFor(u => u.Password)</div>*@
            <div class="editor-field">
                @*//@Html.PasswordFor(u => u.Password)
                    //@Html.ValidationMessageFor(u => u.Password)*@
            </div>
            <input type="submit" value="Log In" />
        </fieldset>
    </div>
}

