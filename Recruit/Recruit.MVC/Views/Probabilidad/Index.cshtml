@model IEnumerable<Recruit.MVC.Models.Probabilidad>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                @*<th>
                    @Html.DisplayNameFor(model => model.Pkprobabilidad)
                </th>*@
                <th>
                    @Html.DisplayNameFor(model => model.Descripcionprobabilidad)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.Pkprobabilidad)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Descripcionprobabilidad)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Pkprobabilidad }) |
                @Html.ActionLink("Details", "Details", new { id=item.Pkprobabilidad }) |
                @Html.ActionLink("Delete", "Delete", new {  id=item.Pkprobabilidad  })
            </td>
        </tr>
}
    </tbody>
</table>
